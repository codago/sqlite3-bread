<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Edit</title>
  <link rel="stylesheet" href="/css/bootstrap.css">
</head>
<body>
  <div class="container">
    <h1><center>Edit</center></h1>
    <form class="form-horizontal" action="<%= item.id %>" method="post">
      <div class="form-group">
        <label for="string" class="col-sm-2 control-label">String</label>
        <div class="col-sm-10">
          <input id="string" type="text" name="string" value="<%= item.string %>" class="form-control">
        </div>
      </div>
      <div class="form-group">
        <label for="integer" class="col-sm-2 control-label">Integer</label>
        <div class="col-sm-10">
          <input id="integer" type="integer" name="integer" value="<%= item.integer %>" class="form-control">
        </div>
      </div>
      <div class="form-group">
        <label for="float" class="col-sm-2 control-label">Float</label>
        <div class="col-sm-10">
          <input id="float" type="float" name="float" value="<%= item.float %>" class="form-control">
        </div>
      </div>
      <div class="form-group">
        <label for="date" class="col-sm-2 control-label">Date</label>
        <div class="col-sm-10">
          <input id="date" type="date" name="date" value="<%= item.date %>" class="form-control" <%= item.date == 'true' ? ' selected' : '' %>>
        </div>
      </div>
      <div class="form-group">
        <label for="boolean" class="col-sm-2 control-label">Boolean</label>
        <div class="col-sm-10">
          <select id="boolean" name="boolean" class="form-control">
            <option value="true"<%= item.boolean == 'true' ? ' selected' : '' %>> True </option>
            <option value="false"<%= item.boolean == 'false' ? ' selected' : '' %>> False </option>
          </select>
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button class="btn btn-primary" type="submit">update</button>
        </div>
      </div>
    </form>
  </div>
</body>
</html>
